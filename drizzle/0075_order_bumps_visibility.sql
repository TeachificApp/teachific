-- Already applied: CREATE TABLE order_bumps, order_bump_conversions, private_invites
-- Already applied: ALTER TABLE digital_products ADD visibility
-- Already applied: ALTER TABLE quizzes ADD visibility
-- This migration file is a no-op marker since the tables were already created.
SELECT 1;
